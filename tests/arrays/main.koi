import io

func (main) none {
  const a = [1, 2, 3]
  io(printLine: a)
  mut b = new Array[Float]{}
  io(printLine: b)

  a = a(append: 4)
  io(printLine: a(length))
  io(printLine: a[1])
  const d = a[2] * 2
  io(printLine: d)
}
